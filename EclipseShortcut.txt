Press Alt + Shift + S + V to quickly implement/override methods in Eclipse.
cltr+shift+o ---> fix import


                                    Session session = getSf().getCurrentSession();
		Transaction tx = session.beginTransaction();
		try {
			
			tx.commit();
		}catch (RuntimeException e) {
		   if(tx!=null)
			   tx.rollback();
		   throw e;
		}